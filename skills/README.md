# Team skills

This directory will hold general team skills that aren't specific to project-kb — things like code review conventions, testing approaches, deployment checklists, or any other LLM instruction files the team wants to standardise.

Currently empty. Add skill domains here as subdirectories, e.g. `code-review/`, `testing/`.
